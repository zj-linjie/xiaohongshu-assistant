# DESIGN.md

## 产品定位

`薄荷工坊 / Mint Atelier` 是一个桌面端 3 列 Web 工作台原型，用于呈现小红书内容创作流程。当前实现是 React + Vite 静态高保真界面，重点是视觉、布局、交互状态和设计 QA，而不是后端生成能力。

首屏必须保持生产力工具的清晰秩序：左侧导航栏、中间主工作区、右侧配置栏。不要把它改成营销落地页、品牌介绍页或普通卡片合集。

## 视觉方向

设计风格是 `Pastel 3D Claymorphism Dashboard`：

- 主色使用马卡龙紫、粉、奶油白、浅蓝、浅黄、薄荷绿。
- 背景使用非常浅的紫粉到奶油渐变，不使用纯白背景。
- 卡片使用 24px 到 32px 大圆角，带柔和外阴影和轻微内高光，形成 3D 黏土凸起感。
- 按钮使用胶囊形状和轻微立体感，主按钮保持粉紫渐变。
- 图标保持圆润 3D soft icon / 软糖质感，不要替换成普通线性图标。
- 整体应该像精致的 3D 玩具工作台，但不能幼稚、混乱或牺牲可读性。

禁止方向：

- 深色科技风。
- 玻璃拟态。
- 极简黑白风。
- 营销落地页 hero。
- 普通线性图标替代 3D soft icon。
- 一味堆卡片导致内容拥挤或层级不清。

## 布局规范

当前画布以 1440px 宽桌面端为基准，根布局为 3 列：

- 左侧导航栏：Logo、用户头像、主导航菜单、项目入口、设置入口。
- 中间主工作区：顶部欢迎/概览卡片、输入灵感、编辑草稿、效果预览、最近任务。
- 右侧配置栏：模型配置、采样参数、本地资源、运行历史。

页面必须支持上下滚动。不要在外层重新引入会裁切内容的 `overflow: hidden`。当前修复后的关键约束是：

- `body` 允许 `overflow-x: auto` 和 `overflow-y: auto`。
- `.app-shell` 使用 `min-height` 而不是固定 `height`。
- `.app-shell` 保持 `overflow: visible`。
- 中间 `.creative-grid`、`.editor`、`.preview` 需要足够的最小高度，避免编辑区、预览区和最近任务被切割、剪裁或遮挡。
- `.preview` 内的小红书 `.post-card` 需要按内容自适应高度，互动数据 footer 必须留在卡片内部，不能被固定高度挤出或遮挡。
- 中间最近任务区域使用内容驱动的最小高度，任务卡和状态控件不能被底部边界压住。

修改布局时必须在 1440x900 和较矮视口中检查：

- 中间内容不被裁切。
- 底部最近任务和左侧设置入口可滚动访问。
- 右侧本地资源和运行历史完整可见或可滚动访问。
- 文本、按钮、卡片互不重叠。

## 组件规范

核心组件和信息层级应保持当前产品形态：

- `sidebar`：品牌、创作者资料、导航、项目列表、设置入口。
- `overview`：今日概览、关键指标、3D 笔记本/铅笔资产。
- `command`：输入灵感、导入素材、选择参考、字数计数、生成内容主按钮。
- `editor`：编辑草稿、分段标签、标题输入、正文编辑、AI 改写工具列。
- `preview`：小红书效果预览、作者、关注与分享操作、封面轮播角标与圆点、标题、正文摘要、话题标签、带图标的互动数据。
- `recent`：最近任务横向任务卡。
- `config-rail`：模型配置、采样参数、本地资源、运行历史。

交互状态至少保持：

- 左侧导航选中态。
- 编辑标签选中态。
- 模型下拉可切换。
- 参数滑杆可调整。
- 输入灵感文本可编辑并更新计数。

如果新增控件，应使用项目现有组件语言：大圆角、柔和阴影、胶囊按钮、清晰选中态和适度留白。

## 资产规范

当前设计依赖这些本地资产：

- `public/assets/avatar-creator.png`
- `public/assets/notebook-pencil.png`
- `public/assets/spring-outfit.png`

新增或替换资产必须匹配当前软糖/黏土 3D 风格，并与马卡龙色系一致。不要使用低质量占位图、普通 emoji、ASCII 图形、粗糙 CSS 绘图或不匹配的 stock 图片。

`notebook-pencil.png` 当前通过 CSS 遮罩与顶部概览卡片融合。如果要进一步提高保真度，应生成透明背景或干净抠图版本，而不是用普通图标替代。

## 验证规范

视觉 QA 以 `design-qa.md` 和 Playwright 截图为证据来源。修改视觉、布局、资产或交互后，至少执行：

```bash
npm run build
```

必要时补充截图验证：

```bash
npx playwright@1.61.1 screenshot --viewport-size=1440,900 http://127.0.0.1:5173/ output/playwright/xhs-g4-1440.png
npx playwright@1.61.1 screenshot --viewport-size=1440,720 --full-page http://127.0.0.1:5173/ /tmp/xhs-g4-scroll-check.png
```

检查重点：

- 1440x900 设计比例是否保持。
- 较矮视口是否可以上下滚动。
- 中间编辑区、预览区、最近任务是否被切割、剪裁或遮挡。
- 文案是否可读，按钮文字是否完整。
- 3D 资产是否加载并与背景自然融合。
- 导航、编辑标签、模型下拉、参数滑杆和输入区是否仍能更新 UI 状态。
