# AGENTS.md

## 项目契约

`薄荷工坊 / Mint Atelier` 是一个 React + Vite 桌面端应用，用于生成小红书图文笔记：5 个可选标题、尾部包含 tag 的正文、以及笔记封面 prompt。

产品保持 3 列工作台形态：左侧导航，中间创作区，右侧配置栏。视觉保持 Pastel 3D Claymorphism，使用马卡龙色、大圆角黏土面板、柔和阴影和 3D soft icon；不要改成营销页、深色科技风、极简黑白风、纯聊天界面或通用 SaaS 后台。

## 核心功能

- 用户输入主题，选择面向人群和文案风格。
- 生成 5 个可选标题、尾部包含 tag 的正文、封面 prompt。
- 支持本地 CLI 模型和云端 API 模型；本地 CLI 默认适配 Codex CLI。
- 支持调用 `xiaohongshu-cli` 获取用户登录状态。
- 用户可以主动调用 `xiaohongshu-cli` 搜索小红书内容。
- 搜索结果必须由用户主动选择后，才加入本地 RAG 知识库。
- 生成文案时可以参考本地 RAG 知识库。

## 风控边界

- `xiaohongshu-cli` 调用必须由用户主动触发，不做后台自动搜索、自动刷新或高频轮询。
- 搜索、采集、入库、生成、发布之间要有清晰的用户确认边界。
- 当前核心范围不包含自动发布、自动评论、自动私信、自动批量采集。
- 生成内容要真实、具体、有信息量，避免空洞夸张、虚假体验、医疗/金融等高风险承诺。
- 封面 prompt 要符合笔记主题和真实使用场景，避免误导性前后对比、夸大效果和平台敏感表达。

## 实现地图

- 应用入口：`src/App.jsx`
- 全局样式和视觉 token：`src/styles.css`
- 资源文件：`public/assets/`

当前 UI 使用本地 React state 驱动可见交互，包括主导航、编辑标签、模型选择、运行方式、Codex 参数、Web Search 开关和灵感输入。实现真实生成链路时，优先沿用现有工作台结构和控件语义。

## 开发命令

```bash
npm run dev -- --port 5173
npm run build
```

## 修改规则

- 产品或交互变化：同步更新本文件。
- 视觉或布局变化：保留当前 3 列工作台和 Pastel 3D Claymorphism 方向。
- 涉及 `xiaohongshu-cli`、搜索、RAG 入库或生成链路时，必须保留用户主动触发和确认边界。
- 完成改动后至少运行 `npm run build`；视觉布局改动还需要截图或浏览器检查。
